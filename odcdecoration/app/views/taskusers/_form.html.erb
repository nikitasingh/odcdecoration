<%= form_for(@taskuser) do |f| %>
  <% if @taskuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taskuser.errors.count, "error") %> prohibited this taskuser from being saved:</h2>

      <ul>
      <% @taskuser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tasks_id %><br />
    <%= f.number_field :tasks_id %>
  </div>
  <div class="field">
    <%= f.label :users_id %><br />
    <%= f.number_field :users_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
