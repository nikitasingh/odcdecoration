<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <% f.label :decoration_id %><br />
    <% f.number_field :decoration_id %>
  </div>

   <% @task.users.each do |user| %> <%=user.username%> <% end %> <br/>
<%= select_tag "user_ids",options_for_select(User.all.collect{|u|[u.username,u.id]},@task.users.map{ |j| j.id }),:multiple => true%>


  <div class="actions">
    <%= f.submit %>
  </div>

  <% end %>